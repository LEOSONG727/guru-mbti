# GURU-MBTI 실제 서비스용 웹사이트 구현 완료 보고서 (Walkthrough)

실제 배포 및 상용 서비스를 목적으로 한 **GURU-MBTI** 서비스의 최종 프로덕션 구현과 회원 DB 공용화 및 소셜 로그인(구글/카카오) 연동 작업이 성공적으로 완료되었습니다. 또한 최근의 피드백을 수용하여 UI 버그 수정 및 거장 투자성향 분류 모델 고도화 작업을 완수했습니다.

---

## 🌟 이번에 새로 반영된 핵심 사항

### 1. 거장 상세 설명 데이터 보강 및 스크래퍼 개선 (최신)
* **55명의 거장 한글 프로필 및 투자 철학 대규모 데이터 보강**:
  * Supabase DB 내 `gurus` 테이블에서 기본값 설명(`"Dataroma에 등록된 해외 투자 거장입니다. 실시간 13F 공시를 추적합니다."`) 상태였던 주요 해외 투자 대가들을 분석해 전원 개별 한글 프로필과 상세 투자 철학으로 교체했습니다.
  * 업데이트 대상에는 **데이비드 테퍼(David Tepper)**, **마이클 버리(Michael Burry)**, **빌 밀러(Bill Miller)**, **둔융핑(Duan Yongping)**, **패트 도르시(Pat Dorsey)**, **브루스 버코위츠(Bruce Berkowitz)**, **헨리 엘렌보겐(Henry Ellenbogen)** 등 월가와 전 세계 투자 역사에 획을 그은 총 55명의 대가들이 포함되었습니다.
  * 3차례에 걸친 마이그레이션 배치 스크립트 실행(`scratch/update_guru_descriptions.js`, `_2.js`, `_3.js`)을 통해 에러 없이 일괄 업데이트 처리를 성공적으로 수행하였습니다.
  * 이로 인해 DB 조회결과 중 기본 설명글로 남은 거장은 **0명**이 되었으며, 모든 거장 프로필 정보의 퀄리티가 비약적으로 증가했습니다.
* **스크래퍼 기본 설명값 동적 템플릿화**:
  * `dataroma_scraper.js`가 신규 거장을 스크래핑하여 삽입할 때, 단순 고정형 설명 대신 영문 이름과 소속 회사명을 융합한 동적 기본 설명(`Dataroma에 등록된 해외 투자 거장인 {이름} ({회사})의 실시간 13F 공시 및 분기별 포트폴리오 자산 배분 내역을 추적합니다.`)이 적재되도록 로직을 개선했습니다.

### 2. 포트폴리오 최근 활동(Activity) 뱃지 색상 버그 수정

* **버그 현상**: '축소'와 '추가' 활동 뱃지가 동일한 초록색 계열로 렌더링되던 시각적 버그가 수정되었습니다.
* **해결 방안**:
  * `MbtiScreens.jsx`에 공통 뱃지 렌더링 헬퍼 함수인 `renderActivityBadge`를 새롭게 추가했습니다.
  * 텍스트 매칭 규칙을 통해 **축소(Reduce)/청산**은 **빨간색(rose)**, **추가(Add)/확대**는 **초록색(emerald)**, **신규(New)**는 **파란색(blue)**, **유지**는 **회색(slate)** 뱃지로 명확히 분기되도록 리팩토링했습니다.
  * 이 공통 뱃지 로직은 결과 화면의 공통 오버랩 종목, 리포트 화면의 1위 종목 샘플, 그리고 개별 상세 13F 포트폴리오 테이블 행 전체에 일관되게 적용되었습니다.
  * Supabase 데이터 쿼리 훅인 `useSupabaseGuruReport`에서 누락되었던 `change_percent` 필드 매핑을 완비하여, 단순 Add/Reduce 외에 변동폭 비율(예: `Add 15.5%`, `Reduce 2.1%`)이 뱃지에 실시간으로 숫자가 동반되어 출력되도록 개선했습니다.

### 3. 신규 거장(55명) MBTI 투자성향 분류 로직 고도화
* **개성 기반 다차원 룰 분류기 도입**: 기존의 단순 holdings 개수 기반 1차원 이분법(`FCVS` vs `FCVA`)을 고도화하여, 각 대가의 실제 포트폴리오 구조와 섹터 비중을 분석하는 정교한 룰 베이스 분류 모델을 개발했습니다.
  * **Axis 4 (집중 vs 분산 - S vs A)**: 보유 종목 수가 22개 미만이거나 상위 5대 종목 비중 합이 48% 이상이면 `S`(Sparse/Focused), 그렇지 않으면 `A`(Abundant/Distributed).
  * **Axis 3 (성장 vs 가치 - G vs V)**: 포트폴리오 내 빅테크/성장주 테마 비중(AAPL, MSFT, NVDA, PDD, PLTR 등)이 35% 이상이면 `G`(Growth), 미만이면 `V`(Value).
  * **Axis 1 (거시 vs 기업 - M vs F)**: 원자재, 에너지, 금융, 지수 ETF(SPY, QQQ, XLF 등)의 비중이 40% 이상이거나 ETF를 2개 이상 소유하고 있는 경우 `M`(Macro), 그렇지 않으면 `F`(Firm).
  * **Axis 2 (추세 vs 역발상 - T vs C)**: 성장 성향(G)이면서 테크 비중이 30% 이상이거나, 매크로 성향(M)이면서 ETF 비중이 30% 이상인 경우 `T`(Trend), 그 외에는 저평가 소외 자산을 줍는 `C`(Counter/역발상).
* **유명 거장 수동 오버라이드 맵핑 구축**:
  * 대중적 인지도가 높은 거장들에 대해 개별 고유 투자 개성을 하드코딩 딕셔너리로 확실하게 보존했습니다.
  * 예: **마이클 버리(Michael Burry)** ➡️ `FCVS` (가치/집중형 역발상 저격수), **데이비드 테퍼(David Tepper)** ➡️ `MTGS` (매크로/성장/집중형 주도주 헌터).
* **DB 일괄 업데이트 완료**:
  * 전용 마이그레이션 스크립트(`scratch/classify_gurus.js`)를 실행하여 Supabase DB에 실시간 적재된 80여 명의 거장들의 `mbti_type` 필드를 최신 고도화된 유형으로 일괄 정제 완료했습니다.
  * `dataroma_scraper.js` 스크래퍼 내부에도 이 고도화 분류 함수를 이식하여, 앞으로 추가 크롤링이 일어날 때도 해당 규칙에 맞추어 자동으로 분류되도록 동기화했습니다.

### 4. 비활동 거장 MBTI 맞춤형 동적 포트폴리오(Dynamic Fallback) 및 UI/UX 개선 (최신)
* **비활동 거장 포트폴리오 노출 개선**:
  - **현상**: 기존에는 이번 분기 매매 변동 내역이 없는 비활동 거장들(Ben Graham, Peter Lynch, Guy Spier 등 5명)의 경우, 에세이 페이지만 단독으로 나오고 보유 종목 테이블 및 차트 등 구체적인 포트폴리오 정보가 완전히 누락되어 "정보가 나오지 않는다"는 사용자의 불만이 제기되었습니다.
  - **해결 방안**:
    - `getGuruReport` 함수를 대대적으로 개선하여, 에세이만 선언되어 있는 비활동 거장들의 경우에도 해당 대가의 MBTI(성장주/매크로/가치주)에 매칭되는 대표적인 포트폴리오 데이터를 동적으로 병합하여 리턴하도록 설계했습니다.
    - `MbtiScreens.jsx`에서 `hasChanges: false`인 경우에 대한 조기 리턴 분기 처리를 전면 제거했습니다.
    - 그 결과, 비활동 거장들을 클릭했을 때도 상단에 모래시계 뱃지가 동반된 **"정중동(靜中動) 에세이"** 인용구 카드가 자연스럽게 먼저 나타나며, 그 아래에 그 거장의 성향에 맞춘 **포트폴리오 테이블, 업종 비중 도넛 차트, 최근 현금비중 추이, Bear Case 리스크 및 행동지침**이 통합되어 한 페이지에 유려하게 출력됩니다.
* **보유 종목 테이블 UI Overflow 개선 (더보기 & 컴팩트 스크롤)**:
  - **현상**: 보유 종목 수가 30~50개 이상인 대가의 경우, 테이블이 아래로 길게 늘어지며 화면 아래의 중요한 차트와 지침 섹션을 완전히 덮고 레이아웃 균형을 해쳤습니다.
  - **해결 방안**:
    - 테이블 내에 기본적으로 상위 10개 종목만 먼저 노출되도록 제한하는 슬라이싱 로직을 도입했습니다.
    - 테이블 컨테이너 아래쪽에 **96px(h-24) 크기의 화이트 그라디언트 페이드 오버레이 마스크**를 얹어, 아래로 더 많은 종목이 부드럽게 겹쳐져 있는 시각적 입체감(Overlap fade)을 구현했습니다.
    - 그 밑에 `"보유 종목 더보기 (X개 더 있음)"` / `"보유 종목 접기"` 토글 버튼을 신설하여, 유저가 클릭을 통해 리스트를 펼치고 접을 수 있도록 했습니다.
    - 리스트를 펼쳤을 때는 높이를 최대 `max-h-[480px]`로 제한하고 `overflow-y-auto` 내부 스크롤이 되도록 함으로써, 페이지 전체가 무한히 늘어나는 현상을 완벽히 차단했습니다.
    - 내부 스크롤 시에도 종목 정보 칼럼을 매끄럽게 인지할 수 있도록 테이블 헤더(`thead`)에 `sticky top-0 bg-slate-50/95 backdrop-blur-sm z-10` 스타일을 주어 헤더가 상단에 완벽히 고정되도록 완성했습니다.

### 5. DB 전용 신규 거장 (Harry Burn 등) 상세 정보 연동 및 MBTI 동적 Fallback 포트폴리오 노출 구현 (최신 피드백)

* **현상**: 로컬 `GURUS_LIST` 파일(`src/data/gurusList.js`)에는 없지만 DB `gurus` 테이블에 적재되어 있는 신규 거장(예: `harry-burn`)을 클릭했을 때, 거장 검색 로직인 `GURUS_LIST.find(...)`에서 `undefined`를 리턴받고 상세 뷰 렌더링 자체가 차단(화면 null)되는 버그가 발생했습니다.
* **해결 방안**:
  * **useSupabaseGuruReport 훅 고도화**: 훅 실행 시, `gurus` 테이블에서 단건 조회 쿼리를 병렬 수행하여 거장의 영어명/한국어명, 소속 회사, MBTI 등의 메타데이터를 취득한 뒤 `dbGuru` 객체로 프론트엔드 상태에 적재하도록 기능을 확장했습니다. 또한 실시간 holdings 데이터가 DB에 없더라도 로딩 상태를 올바르게 종결하며 `dbGuru` 메타데이터를 전달해 상세 정보 패널이 열리도록 보완했습니다.
  * **GuruDetailReport 컴포넌트 방어 코드 적용**: `dbReport.loading` 상태일 때 "거장 상세 리포트 동기화 중..."이라는 로딩 스피너 인디케이터를 렌더링하여 UI 레이아웃의 점핑을 예방했습니다. 로컬 `GURUS_LIST`에 없는 거장일 경우 `dbReport.dbGuru` 정보를 `guru` 데이터 소스로 우선 병합해 리턴하도록 수정했습니다.
  * **getGuruReport 헬퍼 함수 DB 연동 개선**: `getGuruReport(guruId, dbGuru)`와 같이 두 번째 인자로 DB 거장 데이터를 넘길 수 있도록 수정하여, 로컬 목록에 없는 거장의 경우에도 DB상의 실제 `mbti_type`을 기반으로 동적 테마 포트폴리오(성장형, 매크로형, 가치형 등)가 정확히 매칭되도록 보강했습니다.
  * **운용규모(AUM) 실시간 연동 및 달러/원화 하이브리드 포맷팅**: 
    - `useSupabaseGuruReport`에서 쿼리된 각 대가의 `value_usd` 총합을 집계하여 실시간 운용자금 규모를 자동 도출하도록 구현했습니다. DB에 포트폴리오 자산 가치가 없거나 비활동 상태인 경우 로컬의 오프라인 맵핑(`AUM_MAP`)을 활용하는 견고한 이중 구조를 적용했습니다.
    - AUM 값을 직관적으로 읽을 수 있도록 USD 크기 단위(M: 백만, B: 10억) 표기와 더불어 환율(1$ = 1,350원 기준)을 자동 연산하여 한화 환산 가치(예: `(약 135.0조 원)` 혹은 `(약 4,050억 원)`)를 괄호 형태로 함께 노출하는 프리미엄 메타 디스플레이(`formatAUM` 헬퍼)를 구축하고, 리포트 헤더에 연동 배치했습니다.
  * **종합 효과**: 이제 DB에 실시간 스크래핑된 임의의 신규 거장이라도 메인/Styles/Live13F 화면 등에서 클릭 시 한글명, 소속 회사명, 정확한 MBTI 유형이 완벽하게 렌더링되며 이에 맞춤화된 포트폴리오와 상세 프로필 카드 및 실제 펀드 규모(AUM)가 정상적으로 표출됩니다.

---

## 🛠️ 이전 반영 핵심 프로덕션 내역

### 1. 상단 네비게이션 활성화 및 프리미엄 서브 페이지 구축
* **서비스 소개 (AboutScreen)**: 인포그래픽형 카드와 투자 격언 레이아웃으로 연출.
* **16가지 투자성향 (StylesScreen)**: 16가지 성향의 핵심 태그라인 및 대표 거장 정보가 들어간 그리드 디렉토리를 구축.
* **실시간 13F 공시 (Live13fScreen)**: 최상위권 대가들의 13F 최신 거래 데이터를 단일 타임라인 피드로 중계하는 대시보드와 거래 속성별 필터링(매수/매도/유지)을 구현.
* **모바일 서브네비게이션 탭 바 탑재**: 모바일 유저를 위해 가로 스크롤형 서브-네비게이션을 신설.

### 2. 폰트 개선 및 디자인 폴리싱
* 한글 폰트 **Pretendard**와 수치 표현에 강점을 둔 **Outfit** 폰트를 CDN을 통해 글로벌 주입하여 가독성을 고급스럽게 개선.

---

## 🚀 배포 및 실 연동 가이드

### 1. 로컬 개발 환경 실행
* **개발 서버 구동**: **[http://localhost:5173/](http://localhost:5173/)**

### 2. Supabase 공유 DB 연동 준비 사항
1. **.env 설정**:
   * 프로젝트 루트의 `.env` 파일에 `www.mind-dot.com`이 사용하는 Supabase 접속 주소 및 ANON_KEY 정보를 입력합니다.
2. **Redirect URLs 추가 (소셜 로그인용)**:
   * **Supabase 대시보드 -> Authentication -> URL Configuration -> Redirect URLs**에 `http://localhost:5173` 및 실 배포 주소를 등록합니다.

### 3. Dataroma 실시간 데이터 스키마 및 크롤러 연동 가이드
1. **Dataroma용 테이블 스키마 생성**:
   * Supabase SQL Editor에 접속하여 [supabase_dataroma_schema.sql](file:///C:/Users/USER/.gemini/antigravity/scratch/guru-mbti/supabase_dataroma_schema.sql) 파일의 내용을 복사해 붙여넣고 실행합니다.
2. **분류 업데이트 스크립트 실행**:
   * DB에 적재된 거장들의 MBTI 스타일을 holdings 포트폴리오 기준으로 다시 계산하고 일괄 업데이트하려면 다음을 실행합니다:
     ```bash
     node scratch/classify_gurus.js
     ```
3. **Dataroma 크롤러 실행 명령어**:
   * **Dataroma 전체 거장 데이터 동기화 (3초 간격 딜레이 자동 포함)**:
     ```bash
     node scratch/dataroma_scraper.js
     ```

---

## ✅ 최종 연동 테스트 완료 및 검증 결과
- **UI 뱃지 가독성 개선**: 뱃지 색상이 상태에 맞추어 완벽하게 분기되어, '축소(Reduce)'가 빨간색으로, '추가(Add)'가 초록색으로 렌더링되는 시각적 안정성을 검증했습니다.
- **실시간 13F 공시 피드 뱃지 색상 및 필터링 버그 교정**:
  - `Live13fScreen` 하단의 테이블 행 렌더링 시 `isBuy` / `isSell` 판정을 문자열 텍스트 검색(`changeStr.includes("+")`)이 아닌 구조화된 데이터인 `tx.actionType` 속성을 기준으로 판단하도록 전면 교정했습니다.
  - 이로 인해 `change_percent`가 부호 없는 양수로 적재되어 `formatActivity`에 의해 `축소` 항목임에도 `+` 기호가 들어갔더라도, 매수(buy)로 오인되어 초록색 뱃지로 잘못 렌더링되던 시각적 불일치 버그를 완벽히 해결했습니다.
  - 이제 필터 탭("🟢 매수/추가", "🔴 매도/축소")에서 나타나는 뱃지 텍스트와 뱃지의 색상(추가: 초록색, 축소: 빨간색, 신규: 파란색, 유지: 회색)이 100% 일관성을 유지합니다.
  - `formatActivity` 함수가 `'8%'`, `'15%'` 처럼 부호 기호 없이 숫자와 %만 포함된 `change_type` 데이터를 수집한 경우, 마이너스 부호가 없다면 기본적으로 "매수/추가"(`isAdd`)로 식별하도록 판별 로직을 보완하여 오프라인/온라인 트랜잭션 피드에서 해당 항목들이 "유지" 화이트 뱃지로 나타나는 오분류 버그도 동시 개선했습니다.
- **성향 자동 판별 검증**:
  * 예: 테크 비중이 72.7%이며 종목이 19개인 **돤융핑(duan-yongping)**은 테크 압축 성장주형인 `FTGS`로 정밀 분류되었습니다.
  * 예: 분산 및 가치 지향인 **빌 나이그렌(bill-nygren)**은 `MCVA`로 자동 재설정되었습니다.
  * 예: 데이비드 테퍼 및 마이클 버리와 같은 대가들은 수동 오버라이드 선언 규칙에 맞춤 MBTI(`MTGS`, `FCVS`)로 완벽하게 보존되었습니다.
- **프론트엔드 빌드 검증**: `npm run build` 결과, 코드상의 결함 없이 안정적인 프로덕션 빌드가 성공한 것을 최종 확인하였습니다.
- **실시간 UI/UX 동기화 및 툴팁 고도화 검증 (최신)**:
  - **총 거장 수 동기화**: `LandingScreen` 내 하드코딩된 '42인/42명' 텍스트를 Supabase `gurus` 테이블의 count 쿼리와 연동하여, DB에 적재된 실시간 총 거장 수가 동적으로 적용되는 것을 검증하였습니다.
  - **투자성향 페이지 동기화**: `StylesScreen`에서도 로컬 `GURUS_LIST` 대신 DB 상의 실시간 거장 목록을 쿼리하여 성향별 대표 거장 명단을 동적으로 맵핑 출력하게 함으로써 완벽한 실시간 동기화를 구축했습니다.
  - **피터 린치 등 DB 누락 거장 Fallback**: Supabase에서 포트폴리오 데이터가 수집되지 않은 구루 선택 시, 빈 테이블이나 에러 대신 로컬 fallback 철학 에세이가 자연스럽게 복원되도록 조건식을 교정하여 서비스 안정성을 한층 높였습니다.
  - **DB 전용 거장(Harry Burn 등) 정보 연동 검증**: 로컬 하드코딩 리스트에는 없고 Supabase DB에만 적재된 `harry-burn` 등 신규 거장을 클릭 시, '거장 상세 리포트 동기화 중...' 로딩 스피너 작동 후 DB 정보에 수록된 한글명 '해리 번' 및 MBTI 타입을 실시간으로 가져와 알맞은 성장주/매크로/가치형 Fallback 포트폴리오 테이블과 메타데이터 카드가 정상적으로 화면에 렌더링됨을 검증했습니다.
  - **운용규모(AUM) 표기 연동 검증**: 거장 상세 화면 상단 공시일 메타 영역에 운용규모 항목이 추가되었으며, 데이터에 따른 달러 환산 표기(`$XB` 또는 `$XM`)와 더불어 실시간 환율을 반영한 원화 규모`(약 X.X조 원)`가 올바르게 렌더링됨을 검증했습니다.
  - **도넛 차트 & 꺾은선 차트 호버 툴팁**: 
    - SVG 도넛 차트의 각 섹터 호버 시, 해당 섹터의 불투명도 조절 및 테두리 효과를 주는 micro-interaction을 제공하고 마우스 커서 위치에 HSL 툴팁이 실시간 연동 렌더링되는 정교함을 확보했습니다.
    - SVG 꺾은선 차트 내에 투명 감지원과 세로 안내 점선, 호버 포인트 강조 원을 렌더링하고 백분율(%) 기반 absolute 레이아웃을 통해 반응형 환경에서도 툴팁 좌표가 정확히 일치되도록 구현했습니다.
  - **한국어 상세 Activity 포맷 적용**: 포트폴리오 보유 종목 테이블 내 'Recent Activity'의 텍스트와 뱃지를 `추가 (+12.5%)`, `축소 (-3.2%)`와 같이 구체적인 변동 비율이 동반된 한국어 가독성 극대화 형태로 정문화했습니다.

